apiVersion: v1
kind: Service
metadata:
  name: integrity-demo-app
  namespace: runtime-demo
  labels:
    app: integrity-demo-app
spec:
  type: ClusterIP
  selector:
    app: integrity-demo-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
