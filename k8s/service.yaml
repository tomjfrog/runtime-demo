apiVersion: v1
kind: Service
metadata:
  name: runtime-demo-app
  namespace: runtime-demo
  labels:
    app: runtime-demo-app
spec:
  type: ClusterIP
  selector:
    app: runtime-demo-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
